$schema: https://azuremlschemas.azureedge.net/latest/pipelineJob.schema.json
type: pipeline

compute: azureml:lab4-cluster

inputs:
  sampled_data:
    type: uri_folder
    path: azureml:amazon_electronics_features_v1_sampled@latest

jobs:
  split:
    type: command
    component: azureml:split_dataset@latest
    inputs:
      data: ${{parent.inputs.sampled_data}}

  # 1. Normalization for all 3 splits
  normalize_train:
    type: command
    component: azureml:normalize_text@latest
    inputs:
      data: ${{parent.jobs.split.outputs.train}}
  normalize_val:
    type: command
    component: azureml:normalize_text@latest
    inputs:
      data: ${{parent.jobs.split.outputs.val}}
  normalize_test:
    type: command
    component: azureml:normalize_text@latest
    inputs:
      data: ${{parent.jobs.split.outputs.test}}

  # 2. Length Features
  length_train:
    type: command
    component: azureml:length_features@latest
    inputs:
      data: ${{parent.jobs.normalize_train.outputs.out}}
  length_val:
    type: command
    component: azureml:length_features@latest
    inputs:
      data: ${{parent.jobs.normalize_val.outputs.out}}
  length_test:
    type: command
    component: azureml:length_features@latest
    inputs:
      data: ${{parent.jobs.normalize_test.outputs.out}}

  # 3. Sentiment Features
  sentiment_train:
    type: command
    component: azureml:sentiment_features@latest
    inputs:
      data: ${{parent.jobs.normalize_train.outputs.out}}
  sentiment_val:
    type: command
    component: azureml:sentiment_features@latest
    inputs:
      data: ${{parent.jobs.normalize_val.outputs.out}}
  sentiment_test:
    type: command
    component: azureml:sentiment_features@latest
    inputs:
      data: ${{parent.jobs.normalize_test.outputs.out}}

  # 4. TF-IDF (Handled in one job with 3 outputs)
  tfidf:
    type: command
    component: azureml:tfidf@latest
    inputs:
      train: ${{parent.jobs.normalize_train.outputs.out}}
      val: ${{parent.jobs.normalize_val.outputs.out}}
      test: ${{parent.jobs.normalize_test.outputs.out}}

  # 5. SBERT Embeddings
  sbert_train:
    type: command
    component: azureml:sbert_embeddings@latest
    inputs:
      data: ${{parent.jobs.normalize_train.outputs.out}}
  sbert_val:
    type: command
    component: azureml:sbert_embeddings@latest
    inputs:
      data: ${{parent.jobs.normalize_val.outputs.out}}
  sbert_test:
    type: command
    component: azureml:sbert_embeddings@latest
    inputs:
      data: ${{parent.jobs.normalize_test.outputs.out}}

  # 6. Final Merges (The Fix for 'overall' column)
  merge_all:
    type: command
    component: ../components/merge_features/component.yml
    inputs:
      original_data: ${{parent.jobs.split.outputs.train}}
      length: ${{parent.jobs.length_train.outputs.out}}
      sentiment: ${{parent.jobs.sentiment_train.outputs.out}}
      tfidf: ${{parent.jobs.tfidf.outputs.train_out}}
      embedding: ${{parent.jobs.sbert_train.outputs.out}}

  merge_val:
    type: command
    component: ../components/merge_features/component.yml
    inputs:
      original_data: ${{parent.jobs.split.outputs.val}}
      length: ${{parent.jobs.length_val.outputs.out}}
      sentiment: ${{parent.jobs.sentiment_val.outputs.out}}
      tfidf: ${{parent.jobs.tfidf.outputs.val_out}}
      embedding: ${{parent.jobs.sbert_val.outputs.out}}

  merge_test:
    type: command
    component: ../components/merge_features/component.yml
    inputs:
      original_data: ${{parent.jobs.split.outputs.test}}
      length: ${{parent.jobs.length_test.outputs.out}}
      sentiment: ${{parent.jobs.sentiment_test.outputs.out}}
      tfidf: ${{parent.jobs.tfidf.outputs.test_out}}
      embedding: ${{parent.jobs.sbert_test.outputs.out}}